#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s="and wellcame";//class and object
    // cout<<s.size()<<endl;
    // cout<<s.max_size()<<endl;
    // cout<<s.capacity()<<endl;
    // s.clear();
    //  cout<<s.size();
    // string x="king is back";
    // x.clear();
    // (x.empty()==true)? cout <<"empty": cout<<"not empty";
    string g;
    cin>>g;
    g.resize(6);
    g.resize(8,'h');
    cout<<g;
    return 0;
}